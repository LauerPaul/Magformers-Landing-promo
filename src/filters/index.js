export default {
	uppercase (v) {
		return v.toUpperCase()
	},
	capitalize (v) {
		return v.toCapitalize()
	},
	lowercase (v) {
		return v.toLowercase()
	},
	currency (v) {
		return v.toCurrency()
	}
}
